/** @type {import('next').NextConfig} */
const nextConfig = {
  // The 'output: export' configuration has been removed to fix the 404 error in the development environment.
};

module.exports = nextConfig;
